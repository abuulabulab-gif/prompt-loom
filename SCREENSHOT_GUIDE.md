# LOOM スクショ撮影・Gemini指示書

> prompt-loom.com / The Prompt Weaving Studio  
> 公式X: @prompt_loom

---

## 📌 Geminiへの指示（冒頭に渡す文）

```
以下は「LOOM」というAIイラスト向けプロンプト管理WebアプリのX投稿素材です。
機能の説明文と対応スクショ番号をもとに、Xに投稿する文章を作ってください。

トーン：フレンドリーかつ技術的すぎない。初めてAIイラストを触る人にも伝わる言葉で。
文字数：1投稿140字以内を基本。スレッド形式（複数ツイート）でもOK。
ハッシュタグ候補：#AIイラスト #StableDiffusion #NovelAI #Midjourney #プロンプト
URL：https://prompt-loom.com
公式X：@prompt_loom

【アプリの一言説明】
「MJ・NAI・SD・Flux・DALL-E に対応した、キャラ管理もできるプロンプト構築ツール。
ブロックでタグを組み立てて、ワンクリックでコピーするだけ。完全無料・インストール不要。」
```

---

## 🎬 基本の使い方（チュートリアル順）

### STEP 1 ― ツールを選ぶ
画面下部の出力バーにあるツールボタンで使用するAIを選択する。
`汎用 / Midjourney / NovelAI / SD-WebUI / Flux2 / DALL-E` の6種類。
切り替えると出力フォーマットが自動で最適化される。

### STEP 2 ― キャラクターを作る
画面上部のキャラバーで「+ 新キャラ」を押す。
名前・カラー・絵文字を設定してキャラクターを識別。

### STEP 3 ― ブロックでタグを選ぶ
ブロックはプロンプトの「カテゴリ」。
各ブロックを展開してタグをクリックするだけで選択状態になる。
選んだタグは自動でプロンプトに追加される。

**ブロック一覧（デフォルト）**
| ブロック | 内容 |
|----------|------|
| ⭐ ベース・品質 | masterpiece, best quality など |
| 🎨 アートスタイル | アニメ/リアル/水彩/ピクセル etc. |
| 🧬 キャラ属性 | 種族・性別・年齢・特殊パーツ |
| ✨ キャラ・顔 | 髪色・髪型・目・表情・メイク |
| 💫 キャラ・体型 | 体型・肌色・細部 |
| 🎀 衣装 | ジャンル・トップス・素材・アクセ |
| 💍 特徴・アクセ | 武器・ピアス・装備 |
| ✦ エフェクト | 魔法・パーティクル・天候・フィルタ |
| 📐 構図 | カメラ距離・角度・ポーズ・視線 |
| 🌆 背景 | 屋内外・時間・天気・季節 |
| 💡 ライティング | 光源・照明スタイル |
| 🚫 ネガティブ | NG品質・NG身体パーツ |

### STEP 4 ― コピーして使う
出力エリア（画面下部）に生成されたプロンプトが表示される。
「COPY」ボタンまたは `Ctrl+Enter` でコピー。
そのままAIツールに貼り付ける。

---

## 🗂 全機能リスト（洗いざらい）

### ■ プロンプト構築
- **ブロック式タグ選択** — カテゴリ別ブロックでタグをクリック選択
- **タグON/OFF** — 選択済みタグを再クリックで解除
- **強度設定** — 各ブロックに重み（0.8〜1.2）を設定（`(tag:1.2)` 形式）
- **カスタムタグ入力** — ブロック内に自由なタグを手入力で追加
- **タグのピン留め** — よく使うタグを★でブロックトップに固定表示
- **タグ競合チェック** — 「金髪+黒髪」など矛盾タグを自動検出してハイライト
- **カスタムブロック追加** — 自由名称のブロックを追加（PC5個/スマホ3個）
- **ブロックのON/OFF** — ブロック単位でプロンプトへの反映をOFFにできる
- **ブロックのロック** — ロックしたブロックはランダム生成・一括リセットから保護
- **ブロックの非表示** — 使わないブロックをサイドパネルから非表示に（データ保持）
- **ブロックの並び替え** — ドラッグ&ドロップで順序変更
- **ブロック順リセット** — デフォルト順に戻す（テキストは維持）
- **カラーピッカー** — 「水色・インナーカラー」などUIで視覚的に選択→タグ変換
- **フォーカスモード（PC）** — 1ブロックを大きく表示して集中編集

### ■ 出力・コピー
- **ポジティブ出力** — 有効ブロックのタグを結合して表示
- **ネガティブ出力** — ネガティブブロックのみ抽出
- **自然文出力** — 選んだタグから日本語・英語の自然文に変換
- **ワンクリックコピー** — COPYボタン / Ctrl+Enter
- **出力直接編集** — コピー前にプロンプトを手直しできる編集モード
- **トークンカウンター** — 文字数リアルタイム表示（警告色付き）
- **ツール別フォーマット自動調整** — Flux=重み除去、MJ=`--ar`付与、NAI=タグ重みを`{tag:1.2}`形式に変換+LoRA付与、DALL-E=自然文タブへ自動切替
- **サフィックスカスタマイズ** — `--ar 16:9 --v 6` などをツールごとに編集保存

### ■ AIツール対応
- 汎用（カンマ区切り）
- Midjourney（スペース区切り、`--ar --v` サフィックス）
- NovelAI（カンマ区切り、LoRA `{name:weight}` 形式）
- SD-WebUI / Forge / ComfyUI（カンマ区切り、LoRA `<lora:name:weight>` 形式）
- Flux2（重み記法を自動除去）
- DALL-E / Copilot（選択時に自動で自然文タブへ切替）

### ■ バリエーション生成
- **バリエーション3種自動生成** — ベース（顔・体型）は固定、衣装・構図をランダムに変化
- **各バリエーションの自然文表示** — 行ごとに🗣ボタンで自然文/プロンプト切替
- **バリエーション再生成** — ↻ボタンで別パターンを再生成
- **バリエーションコピー** — 各行の📋でそのままコピー

### ■ ランダム生成
- **おまかせランダム生成** — 🎲ボタンで矛盾のないキャラを一発生成
- **種族連動** — エルフ選択で「エルフ耳」が自動追加、など
- **ロック保護** — ロックブロックはランダム対象外

### ■ キャラクター管理
- **複数キャラ管理** — キャラバーでタブ切替
- **キャラ複製** — ⊕で既存キャラを複製
- **キャラ削除** — ✕で削除
- **キャラアーカイブ** — 削除せずに格納庫へ移動
- **キャラライブラリ** — フォルダ分け・検索・アーカイブ閲覧
- **キャラバージョン管理** — 最大10件のスナップショットを保存・復元
- **キャラ比較（Compare）** — 別キャラのプロンプトを横並びで比較
- **ブロック転送** — 特定ブロックの内容を別キャラにコピー
- **サムネイル登録** — キャラごとに画像を最大4枚登録（自動圧縮）
- **キャラメモ** — LoRA名・使用モデル・コツなどを自由記述
- **LoRA管理** — LoRA名と重みを登録→SD/NAI出力に自動追加
- **URLシェア** — キャラプロンプトをURLに変換して共有、受信者は1クリックでインポート

### ■ プリセット・テンプレート
- **衣装プリセット** — よく使う衣装タグセットを保存・ロード
- **構図プリセット** — よく使う構図タグセットを保存・ロード
- **プリセット共有** — 別キャラへのコピーも可
- **テンプレート適用** — 「セーラー服」「ファンタジー戦士」などのプリセットスタイルをワンクリック適用
- **プリセットJSON書き出し/読み込み** — 衣装・構図プリセットのみを別ファイルで共有可能

### ■ シーン合成
- **マルチキャラシーン** — 複数キャラを選んで位置（左/中/右/後ろ）とインタラクション（見つめ合う・手をつなぐ等）を指定
- **合成プロンプト生成** — BREAK構文対応（NovelAI/SDXL推奨）
- **最大3キャラまで選択可能**

### ■ 履歴・スナップショット
- **自動履歴** — COPY時に自動保存（最大20件）
- **手動スナップショット** — 📸ボタンで任意タイミングで保存
- **履歴から復元** — 過去の状態をワンクリックで復元
- **プロンプト逆解析（Analyze）** — 外部のプロンプト文字列を貼り付けて、どのブロックに対応するかを可視化

### ■ キャラノート
- **プロフィールシート** — 10セクション（基本情報・性格・関係性・世界観 etc.）＋カスタム項目
- **プロンプトログ** — COPYしたプロンプトを自動記録・タグ・メモ付き管理
- **タグ対応表（TagMap）** — キャラ固有のタグ対応メモ表

### ■ UI・表示
- **表示モード3段階** — Simple（初心者向け主要ブロックのみ）/ Normal / Expert（全ブロック＋追加機能）
- **1/2/3列レイアウト（PC）** — ブロックの列数を切替
- **ダーク/ライトテーマ** — テーマ切替
- **言語切替** — UI表示を日本語/英語切替
- **折りたたみ** — ブロックを個別または一括折りたたみ（`[` / `]` キー）
- **スマホ対応** — ブロックジャンプサイドバー、最適化されたレイアウト

### ■ ショートカット
| キー | 機能 |
|------|------|
| `Ctrl+Enter` | コピー |
| `Ctrl+K` | コマンドパレット |
| `Ctrl+F` | 全ブロック横断タグ検索 |
| `H` | 履歴 |
| `T` | テンプレート |
| `G` | グローバル検索 |
| `A` | プロンプト逆解析 |
| `1`〜`6` | AIツール切替 |
| `P` / `N` | ポジティブ/ネガティブタブ |
| `[` / `]` | 全ブロック折りたたみ/展開 |
| `?` | 設定・ショートカット一覧 |
| `Esc` | モーダルを閉じる |

### ■ データ管理
- **自動保存** — IndexedDB（ブラウザローカル）に常時保存
- **JSONエクスポート** — 全キャラ・全データをJSONで書き出し
- **JSONインポート** — 別端末・バックアップから読み込み
- **PWAインストール** — スマホ・PCにアプリとしてインストール可能
- **オフライン動作** — サーバー不要、ブラウザだけで完結

---

## 📸 スクショ撮影指示書

### 撮影の基本設定
- **ブラウザ**: Chrome推奨（フルスクリーン）
- **解像度**: 1280×800 以上
- **ダークモード**: 基本はダークで撮影。比較用にライトも1枚
- **キャラデータ**: 事前にタグをいくつか入れた状態で撮る（空状態NG）
- **URL非表示**: ブラウザのアドレスバーを隠す or 全画面モードで撮影

---

### 📸-01 メイン画面全体（看板ショット）
**目的**: アプリの第一印象。Xのメイン投稿に使う一番重要なカット。

**事前準備**:
- キャラ「桜花　凛」など日本語名で作成
- 顔・衣装・構図ブロックに10〜15タグを入れた状態
- 出力エリアにプロンプトが出ている状態
- テーマ: ダーク

**撮影方法**:
- ブラウザ全画面（F11）
- 画面全体をキャプチャ
- ヘッダー〜ブロック3〜4個〜出力バーまでが入るように

**映したいもの**:
- LOOMロゴ（左上）
- ブロック一覧（タグがONになっているもの複数）
- 下部の出力エリア（プロンプトが埋まっている）

---

### 📸-02 ブロック展開・タグ選択のアップ
**目的**: 「タグをクリックするだけ」の直感性を見せる。

**事前準備**:
- キャラ・顔ブロックを展開した状態
- 髪色カテゴリで「銀髪」「金髪」など数タグが緑ハイライト（選択済み）
- 目の色も1タグ選択済み

**撮影方法**:
- ブロックが画面の大半を占めるようにスクロール調整
- 1ブロック〜2ブロックが見える状態でキャプチャ

**映したいもの**:
- カテゴリ区切り（「髪色」「目の色」等のラベル）
- 選択済みタグの色付きハイライト
- 未選択タグとの差

---

### 📸-03 マルチAIツール切替
**目的**: MJ/NAI/SD/Flux全対応を一枚で証明する。

**事前準備**:
- 同じキャラのまま（タグは同じ）
- ツールを切り替えながら出力を見せる
- **理想**: 2ショット比較（MJ出力 vs NAI出力）— 画面分割か2枚連続

**撮影方法①（1枚）**:
- 出力バーのツールボタン列が全部見える状態
- 現在選択中のツール（例: Midjourney）がハイライトされている
- 出力エリアに `--ar 3:2 --v 8` が付いたMJ形式のプロンプトが見える

**撮影方法②（2枚セット）**:
- 1枚目: MJ選択状態 + 出力に `--ar` サフィックス
- 2枚目: Flux選択状態 + 出力から重み記法が消えている

**映したいもの**:
- ツール名ボタン（🟦MJ、📘NAI、🖼SD、⚡Flux、🎨DALL-E）
- 出力テキストの違い

---

### 📸-04 バリエーション3案パネル
**目的**: 「同じキャラで3パターン自動生成」を見せる。

**事前準備**:
- キャラに顔・衣装・構図ブロックのタグを入れた状態
- 「🎲 バリエ」ボタンを押してパネルを展開した状態
- 1番の行を🗣ボタンで自然文に切替（残り2件はプロンプト表示のまま）

**撮影方法**:
- 出力バーのバリエーションパネルが全部見えるようにスクロール
- バリエーションパネル全体＋出力エリア上部が入るよう

**映したいもの**:
- 3行のバリエーション（番号付き）
- 1行目が自然文（緑文字）、他はプロンプト形式
- 右端の🗣📋ボタン

---

### 📸-05 ランダム生成（おまかせ）
**目的**: 「ボタン1つでキャラができる」インパクト。

**事前準備**:
- キャラを空の状態からスタート
- 🎲おまかせボタンを押した直後の状態
- ブロックが全部タグで埋まっている状態

**撮影方法**:
- キャラパネルの🎲ボタンが見える位置
- その下にブロックがタグで埋まっている状態

**映したいもの**:
- 🎲おまかせボタン（詳細パネル内、もしくはキャラパネル）
- 顔・衣装・構図ブロックにタグが自動入力されている状態

---

### 📸-06 複数キャラ管理 ＋ キャラライブラリ
**目的**: 「複数キャラを同時管理できる」を示す。

**事前準備**:
- キャラを3〜4体作成（名前・カラー・絵文字を各自設定）
- キャラバー（横スクロールタブ）に複数のキャラチップが並んでいる状態
- 📚ライブラリボタンを押してライブラリモーダルを開く

**撮影1（キャラバー）**:
- ヘッダー＋キャラバーが見えるようにキャプチャ
- 選択中キャラがカラーハイライトされている

**撮影2（ライブラリモーダル）**:
- モーダルが開いた状態
- キャラ一覧が並んでいる（絵文字・名前・フォルダ）
- フォルダタブがあると尚良い

---

### 📸-07 シーン合成モーダル
**目的**: 「複数キャラの合成プロンプト」というユニーク機能。

**事前準備**:
- 2キャラ以上作成済み
- ✦ツール → 🎬シーン合成 で開く
- 2キャラを選択し、インタラクション「見つめ合う」などを選択
- 下部に合成プロンプトが表示されている状態

**撮影方法**:
- モーダル全体が見えるようにキャプチャ
- キャラ選択チェックマーク + プロンプト出力両方見える

**映したいもの**:
- 2キャラのカード（絵文字・名前）
- インタラクション選択部分
- 生成された合成プロンプト

---

### 📸-08 コマンドパレット（Ctrl+K）
**目的**: 「キーボードだけで全機能アクセス」を見せる。

**事前準備**:
- Ctrl+K を押してパレットを開く
- 何も入力しない状態（全コマンドが表示されている）

**撮影方法**:
- モーダルが画面上部1/3くらいから出ているところをキャプチャ
- 背景のブロック画面が薄く見えていると奥行きが出る

**映したいもの**:
- 検索ボックス（⌘マーク）
- グループ分けされたコマンド一覧（パネル、AIツール、アプリ設定…）

---

### 📸-09 キャラノート（プロフィール）
**目的**: 「プロンプト管理だけじゃない、キャラ管理ツール」を示す。

**事前準備**:
- エディタ→ノートタブへ切替（📖キャラノートボタン）
- プロフィールシートにある程度情報を入力済み
- （名前・年齢・性格・関係性 etc.）

**撮影方法**:
- プロフィールシートのセクションが複数見えるようにスクロール位置調整
- テキストが入っている状態で撮る（空欄NG）

**映したいもの**:
- セクション区切り（折りたたみ可能なアコーディオン）
- 入力済みのキャラ設定テキスト

---

### 📸-10 自然文出力
**目的**: 「タグが人間の言葉になる」驚き。

**事前準備**:
- 顔・衣装・構図にタグを入れた状態
- 出力タブを「🗣 自然文」に切替
- 言語を日本語に設定

**撮影方法**:
- 出力バーを大きく見せる（出力エリアをドラッグで拡大しておく）
- 自然文が複数行表示されている状態

**映したいもの**:
- 緑色の自然文テキスト（日/英切替ボタンも見える）
- 上部のPositive/Negative/自然文タブ

---

### 📸-11 ダーク/ライト比較（おまけ）
**目的**: テーマ対応のアピール。

**撮影方法**:
- 同じ状態でダークとライトの2枚を撮って並べる
- またはライトモードで画面全体を1枚

---

## 🗒 撮影チェックリスト

```
□ 01 メイン全体（ダーク）
□ 02 ブロック展開・タグ選択アップ
□ 03-A ツール切替（MJ選択状態）
□ 03-B ツール切替（Flux選択状態）
□ 04 バリエーション3案パネル（自然文混在）
□ 05 おまかせランダム生成後
□ 06-A キャラバー（複数キャラ）
□ 06-B キャラライブラリモーダル
□ 07 シーン合成モーダル
□ 08 コマンドパレット（Ctrl+K）
□ 09 キャラノート・プロフィール
□ 10 自然文出力タブ
□ 11 ライトモード全体（比較用）
```

---

## 💎 こだわった部分（Gemini・X投稿ネタ用）

設計や実装で意識して作り込んだポイント。投稿の差別化や「なぜ作ったか」の文脈として使える。

### 🧠 矛盾しないランダム生成
「おまかせ」はただのランダムではない。種族タグ（エルフ・猫耳など）を選ぶと関連パーツ（エルフ耳・猫しっぽ）が自動で追加される。カテゴリ間の排他ルールも組んであり、「金髪＋黒髪」のような矛盾が起きない。

### 🔄 バリエーション生成のロジック
3パターンのバリエーションは「顔・体型は固定、衣装・構図系はランダム変化」という設計。キャラのアイデンティティを壊さずに試行錯誤できるようにした。

### ♾ 新タグが既存データに自動反映
アプリのアップデートで新しいタグを追加したとき、既存ユーザーのキャラデータに自動でマージされる。手動で更新しなくても常に最新のタグが使える。

### 🔒 ブロックロックとセクション制御
「この顔設定は変えたくないけど衣装だけ試したい」というニーズに応えるロック機能。ランダム生成も一括リセットもロックされたブロックをスキップする。

### 📡 URLシェアの設計
プロンプトをURLに変換して共有する機能は、**サーバーなしで完結する**設計にこだわった。データはBase64エンコードしてURLパラメータに乗せるため、バックエンド不要・プライバシー問題なし。受信者はURLを開くだけで相手のキャラをインポートできる。

### 💾 ローカルファースト設計
データはすべてブラウザのIndexedDB（Dexie）に保存。サーバーに一切送信されない。オフラインでも動作し、会員登録も不要。画像（サムネイル）もCanvas APIで圧縮してローカルに保存。

### 🎨 自然文生成のブロック対応
自然文は単純な「タグの日本語訳」ではなく、ブロックの意味を理解して文章構造を組み立てている。顔ブロックの情報を主語に、衣装・構図・背景を文脈に沿って繋げる独自ロジック。

### 🖥 3段階の表示モード
Simple（必要最低限のブロックだけ）→ Normal → Expert（全ブロック＋カスタムブロック追加等）と、習熟度に応じてUIが変わる。初心者が最初から全ブロックで圧倒されないように設計した。

### ⌨ キーボードファースト操作
Ctrl+K のコマンドパレット、Ctrl+Enter でコピー、1〜6でツール切替など、マウスなしで全機能にアクセスできる。ヘビーユーザーが「手を止めない」ことを意識した。

### 📱 スマホでも妥協しないレイアウト
600px未満でレイアウトが切り替わり、タブバー・ジャンプサイドバー・出力バーがすべてスマホ操作に最適化されている。PWAインストールでホーム画面から起動もできる。

---

## 🐦 X投稿構成案（Gemini渡し用）

```
【投稿1 - 看板】01のスクショ
AIイラスト用プロンプト構築ツール「LOOM」を公開しました。

✅ ブロックでタグを選ぶだけ
✅ MJ / NAI / SD / Flux / DALL-E 全対応
✅ 複数キャラを一元管理
✅ 完全無料・登録不要

→ prompt-loom.com
#AIイラスト #NovelAI #StableDiffusion #Midjourney

【投稿2 - スレッド】03のスクショ
ツールを切り替えるだけで出力フォーマットが自動最適化されます。
MJは --ar サフィックス、Fluxは重み記法を自動除去、NAIはタグ重みを{}形式に変換+LoRA自動付与、DALL-Eは自然文タブへ自動切替。
同じキャラを異なるAIで使いまわせます。

【投稿3 - スレッド】04のスクショ
バリエーション生成が便利です。
ボタン1つで3パターンのプロンプトを自動生成。
顔・体型は固定のまま、衣装・構図だけランダムに変化します。
🗣ボタンで自然文にも切替できます。

【投稿4 - スレッド】05のスクショ
🎲おまかせボタンで、矛盾のないキャラプロンプトを一発生成できます。
「エルフ」を選ぶとエルフ耳が自動で追加されるなど、種族連動もあり。

【投稿5 - スレッド】07のスクショ
複数キャラの合成プロンプトも作れます。
2〜3キャラを選んで位置・インタラクションを指定するだけ。
NovelAI/SDXLのBREAK構文対応。

【投稿6 - スレッド】09のスクショ
プロンプト管理だけじゃなくキャラ管理もできます。
設定シート・プロンプトログ・タグ対応表をキャラごとに管理。
LoRA登録→出力に自動追加する機能もあります。
```
