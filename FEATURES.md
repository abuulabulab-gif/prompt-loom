# LOOM 機能一覧 — Xプレゼン素材

> prompt-loom.com / The Prompt Weaving Studio — AIイラストプロンプト構築・キャラ管理Webアプリ
> 公式X: https://x.com/prompt_loom
> Gemini整理用の全機能ダンプ。★= X投稿おすすめ度

---

## 🌟 イチオシ（X投稿最優先）

### ★★★ マルチAIツール対応（ワンクリック切替）
Midjourney / NovelAI / SD-WebUI / Flux2 / DALL-E の5ツールに対応。
切り替えるだけでプロンプト形式が自動最適化される（Flux=重み記法除去、MJ=`--ar`サフィックス自動付与、NAI=LoRAフォーマット変換など）。
→ **スクショ案**: ツール切替ボタン列 + 同じキャラで出力が変わる比較

### ★★★ ブロック式プロンプト構築
タグを「品質 / アートスタイル / 顔 / 体型 / 衣装 / 構図 / 背景 / ライティング / エフェクト」等のブロックに分けて管理。
各ブロックを ON/OFF するだけでプロンプトが即時更新される。
→ **スクショ案**: ブロック一覧（タグがカテゴリ分類されている様子）

### ★★★ バリエーション生成
ボタン1つで3パターンのプロンプトバリエーションを自動生成。
ベースキャラ（顔・体型）は固定したまま、衣装・構図だけをランダムに変化させる。
→ **スクショ案**: バリエーション3案が並んだパネル

### ★★★ ランダムキャラ生成
🎲ボタンで矛盾のないキャラプロンプトを一発生成。
種族タグ連動（例: エルフ→エルフ耳が自動追加）。
→ **スクショ案**: 生成前後のブロック比較 or 生成されたプロンプト

### ★★★ 複数キャラ管理 + キャラライブラリ
複数のキャラをフォルダ管理でき、切り替えはタブ1クリック。
キャラごとに全ブロック・LoRA・プリセット・ノートが独立保存される。
→ **スクショ案**: キャラライブラリモーダル（フォルダタブ付き一覧）

---

## ★★ 注目機能（スレッドやサブ投稿向け）

### シーン合成（Scene Compose）
複数キャラを選んで位置関係・インタラクションを指定 → マルチキャラプロンプトを自動生成。
`BREAK`構文（NovelAI / SDXL）対応。
→ **スクショ案**: 2-3キャラ選択してプロンプト生成されているモーダル

### URLシェア機能
キャラのプロンプトをURLに変換してシェア。
受信者はURLを開くだけでキャラがインポートされる（インストール不要）。
→ **スクショ案**: シェアボタン + URLコピー完了状態

### スナップショット / 履歴（最大20件）
コピー時に自動で履歴保存。手動スナップショットも可能。
過去のプロンプトを1クリックで復元できる。

### キャラノート（CharacterNote）
キャラごとに設定シート（10セクション+カスタム項目）、プロンプトログ、タグ対応表を管理。
生成した画像メモとしてサムネイル最大4枚登録可能。

### カラーピッカー
「水色インナーカラー」「紫アイシャドウ」などを視覚UIで選択 → タグに自動変換。

### コマンドパレット（Ctrl+K）
VSCodeライクなコマンドランチャー。全機能にキーボードだけでアクセス可能。
主なショートカット: `Ctrl+Enter`=コピー、`Ctrl+F`=全ブロック検索、`H`=履歴、`T`=テンプレート

### 全ブロック横断タグ検索（Ctrl+F）
どのブロックに入っているかに関わらず、タグ名で一括検索・トグル可能。

### 表示モード切替（Simple / Normal / Expert）
- Simple: 主要ブロックのみ表示（初心者向け）
- Normal: 通常表示
- Expert: 全ブロック＋詳細タグ全開

### タグ競合チェック
「白髪 + 黒髪」などの矛盾するタグを自動検出してハイライト表示。

---

## ★ 細かいけど便利な機能

| 機能 | 説明 |
|------|------|
| LoRA対応 | SD/NAI向けLoRA文字列（`<lora:name:weight>`）を自動生成・出力 |
| ピン留めタグ | よく使うタグをブロックトップにピン留め（★ボタン） |
| ブロックロック | 「このブロックは変えない」ロックでランダム生成やリセットから保護 |
| ブロック非表示 | 不要なブロックをサイドバーから非表示に（データは保持） |
| D&Dブロック並替 | ドラッグ＆ドロップでブロック順序をカスタマイズ |
| カスタムブロック | 自由名称のカスタムブロックを追加（PC5個/スマホ3個） |
| プリセット保存 | 衣装・構図のプリセットをキャラごとに保存・ロード |
| テンプレート | 既成スタイルをワンクリック適用（セーラー服・ファンタジー等） |
| 自然文出力 | 日本語・英語の自然な文章としてプロンプトを出力 |
| トークンカウンター | 文字数をリアルタイム表示（警告色あり） |
| 出力エリア直接編集 | コピー前にプロンプトを直接テキスト編集できる |
| キャラバージョン | 過去のキャラ状態を最大10件保存・復元 |
| JSON書き出し/読み込み | 全データをJSONでバックアップ＆別端末への移行 |
| プリセット書き出し | 衣装・構図プリセットのみをJSONで共有 |
| ダーク/ライトテーマ | テーマ切替対応 |
| PWAインストール | スマホ・PCにアプリとしてインストール可能 |
| 自動保存 | IndexedDB（オフライン）に常時自動保存（クラウド不要） |
| フォーカスモード | 特定ブロックを拡大表示してタグ選択に集中 |
| ブロック転送 | 選択したブロックの内容を別キャラにコピー |
| キャラアーカイブ | 使わなくなったキャラをアーカイブ（削除せず保管） |

---

## 🖥️ 対応環境

- **完全無料・会員登録不要**
- ブラウザのみ動作（オフライン対応）
- スマホ・PC両対応（レスポンシブ）
- PWAインストール対応
- データはすべてローカル保存（サーバー送信なし）

---

## 📸 スクショ撮影リスト（X投稿用）

優先順に:
1. **メイン画面全体** — ブロック一覧 + 出力エリア（PCとスマホ両方）
2. **マルチツール切替** — ツールボタン + 同キャラでフォーマットが変わる様子
3. **バリエーション3案パネル** — 「shuffle」ボタン押下後
4. **キャラライブラリ** — フォルダ分けされた複数キャラ一覧
5. **シーン合成モーダル** — 2キャラ選択してプロンプト生成
6. **キャラノート** — プロフィールシートとプロンプトログ
7. **コマンドパレット** — Ctrl+K で開いた状態
8. **ランダム生成結果** — 🎲ボタン後のブロック埋まった状態
9. **ダーク/ライト比較** — テーマ切替の見た目差

---

## 🐦 X投稿フレーズ案

### 紹介ツイート（メイン）
```
AIイラスト用プロンプト管理ツール「LOOM — The Prompt Weaving Studio」を公開しました🎉

✅ MJ / NovelAI / SD / Flux / DALL-E 全対応
✅ ブロック式でキャラを組み立て → ワンクリックコピー
✅ 複数キャラを一元管理
✅ 完全無料・インストール不要・データはローカル保存

→ prompt-loom.com
```

### サブ投稿（バリエーション機能）
```
LOOMはバリエーション生成が便利です

顔・体型は固定のまま、衣装・構図だけが変わる
プロンプト3案を自動生成します

同じキャラで複数パターン試したいときに
```

### サブ投稿（マルチキャラ）
```
複数キャラの合成プロンプトも生成できます

キャラAとBを選んで「手をつなぐ」「背中合わせ」など
インタラクションを指定するだけ

NovelAI/SDXLのBREAK構文にも対応
```
