# キャラノート機能 仕様書

> prompt-loom.com — AIイラストプロンプト管理アプリ  
> 対象バージョン: v1.8相当（2026-05-27現在）

---

## 概要

「キャラノート」は、キャラクターごとに紐付く3タブ構成のサブ機能。  
メインのエディタとは独立したビュー（`mainTab === 'note'`）として表示される。

```
CharacterNote/
├── index.jsx         タブ切替 (profile / log / tagmap)
├── ProfileSheet.jsx  設定シート
├── PromptLog.jsx     プロンプトログ（一覧 + 操作）
├── PromptLogEntry.jsx  ログ1件分のUI
├── TagMap.jsx        タグ対応表
└── RecordModal.jsx   手動記録モーダル
```

### データ格納先

すべてキャラクターオブジェクト（IndexedDB）の中に格納。  
クラウド同期（Firestore）にも含まれる。

| フィールド | 型 | 用途 |
|---|---|---|
| `char.profile` | `{ [sectionId]: { [fieldKey]: string }, customFields: CustomField[] }` | 設定シートデータ |
| `char.promptLog` | `LogEntry[]` | プロンプトログ |
| `char.tagMap` | `TagMapRow[]` | タグ対応表 |

---

## 1. 設定シート（ProfileSheet）

### 目的

キャラの設定・外見・口調・衣装などをメモする参照用シート。  
AI画像生成のプロンプト作成時のリファレンスとして使う。

### セクション構成（全10 + カスタム）

| セクションID | アイコン | 日本語名 | フィールド数 | デフォルト開閉 |
|---|---|---|---|---|
| `basic` | 📌 | 基本情報 | 9 | **開** |
| `appearance` | 🎨 | 外見設定 | 20 | **開** |
| `speech` | 💬 | 話し方・感情 | 9 | 閉 |
| `lifestyle` | 🏠 | 生活スタイル | 5 | 閉 |
| `likes` | ❤️ | 好き・嫌い・趣味 | 4 | 閉 |
| `relations` | 🔗 | 関係性・持ち物 | 3 | 閉 |
| `special` | ⚔️ | 特殊設定・能力 | 3 | 閉 |
| `outfit` | 👗 | 衣装・見た目 | 4 | **開** |
| `ai` | 🤖 | AI用メモ | 2 | **開** |
| `memo` | 📝 | 背景・その他メモ | 1 | 閉 |
| `(custom)` | ✏️ | カスタム項目 | 可変 | **開** |

`appearance`セクションの主要フィールド例:
`hair / bangs / inner_color / eyes / eye_shape / eyebrow / expression / skin / nail / ear / body / height_child / height_adult / bust / special / color_main / color_sub / accessories / posture / other`

### フィールド種別

- **1行 input**: デフォルト。短い値向け
- **複数行 textarea（multi: true）**: 衣装詳細・変身・関係性など、長文が必要なフィールド
- **カスタムフィールド**: ユーザーが任意に追加可能。1行/複数行を切替ボタンで変更できる

### UI動作

- 各セクションはアコーディオン（クリックで開閉）
- **開閉状態は `localStorage` に `loom_note_sections_${char.id}` キーで保存**（キャラ切替時に復元）
- フィールド編集はリアルタイムで `onUpdate` → IndexedDB保存（デバウンスあり）
- 「📋 設定をテキストコピー」ボタン: 入力済みフィールドのみを日本語/英語で整形してクリップボードへ
  ```
  【キャラ名】
  
  ▶ 基本情報
  種族・出自: エルフ
  性格: 無口だが内心は世話焼き
  ...
  ```

### 制約・現状の限界

- セクションの並び順はハードコード（ユーザーが変更不可）
- フィールドラベルもハードコード（日本語/英語のみ）
- カスタムフィールドの並び替えは不可（追加順固定）
- 画像添付不可
- セクション間でのフィールド移動不可
- 設定シートのデータとブロックタグの間に自動連携なし（手動参照）

---

## 2. プロンプトログ（PromptLog / PromptLogEntry）

### 目的

過去に生成・試したプロンプトを記録・整理して再利用しやすくする。  
「何を試したか」「どんな結果だったか」を時系列で把握する。

### ログエントリのデータ構造

```ts
type LogEntry = {
  id: string;           // ランダムUID
  ts: number;           // 作成日時（Unix ms）
  title: string;        // タイトル（手動入力）
  tool: string;         // AIツール (MJ / NAI / SD / flux / dalle / general)
  labels: string[];     // 任意ラベル配列
  posText: string;      // ポジティブプロンプト（記録時点のテキスト）
  negText: string;      // ネガティブプロンプト
  memo: string;         // メモ・印象
  blocks?: SlimBlock[]; // ブロック設定スナップショット（cats/lastRandomPicks除去済み）
  editedAt?: number;    // 最終編集日時（編集した場合のみ）
};
```

`blocks`はスリム化済み（`cats`・`lastRandomPicks`は除去）。  
復元時は `mergeCharacterBlocks()` で `cats` を再構築する。

### 記録方法

#### 自動記録（Auto-log）
- メインエディタの「COPY」ボタン押下時に自動で記録
- 自動タイトルは `YYYY/MM/DD HH:MM` 形式
- `localStorage: loom_autolog_copy` で ON/OFF 切替（デフォルト ON）
- **上限100件**: 超えたときはトースト通知（コピー自体は実行される）

#### 手動記録（RecordModal）
- ログタブの「＋ 現在のプロンプトを記録」ボタン → モーダル
- 入力項目: タイトル / ツール選択 / ラベル（複数）/ メモ
- 過去に使ったラベルをワンクリックで再利用できる「既存ラベル候補」表示
- Enter で保存 / Escape でキャンセル

### フィルタ・検索

| フィルタ | 対象 |
|---|---|
| ツールフィルタ | `entry.tool` の完全一致 |
| ラベルフィルタ | `entry.labels` に含まれるか |
| テキスト検索 | `title` / `memo` / `posText` の部分一致（小文字化して比較） |

- 複数フィルタは AND 条件
- ✕ボタンで全フィルタ一括クリア

### 一覧表示

- 日付でグループ分け（`YYYY/M/DD`形式ヘッダー）
- 各エントリは折りたたみ表示（デフォルト閉）
  - **閉じた状態**: 時刻 / タイトル / ツール / 「編集済み」バッジ / ラベル / posText冒頭100文字プレビュー
  - **開いた状態**: Positive / Negative / メモ + 操作ボタン群

### エントリ操作

| 操作 | 動作 |
|---|---|
| ↩ ブロックを復元 | `mergeCharacterBlocks(entry.blocks)` で現在のブロック設定を上書き復元（確認ダイアログあり） |
| 📋 コピー | `entry.posText` をクリップボードへ（negTextはコピーされない） |
| ✏️ 編集 | タイトル / メモ / posText / negText をインライン編集。保存時に `editedAt` セット |
| 🗑 削除 | 確認ダイアログ → 即時削除 |

### 制約・現状の限界

- **上限100件**（超過時は自動記録スキップ、手動記録はアラート）
- 一括削除・全件クリア機能なし
- エクスポート（CSV/JSON等）機能なし
- 自動記録で同じプロンプトの重複記録が起きやすい（内容変化の検知なし）
- negText は検索対象外
- 📋コピーは posText のみ（negText を含むコピーはできない）
- ラベルは文字列配列のみ（色・グループ分けなし）
- 復元時は「ブロック設定のみ」（posText / negText は自動適用されない）
- `editedAt` はあるが、編集前の原文を保持しない（undo不可）

---

## 3. タグ対応表（TagMap）

### 目的

キャラの設定表現（日本語）と AI プロンプトタグ（英語）を対応付けて管理する。  
例: 「銀黒の長髪」→ `silver hair, black inner color, long hair`

### データ構造

```ts
type TagMapRow = {
  id: string;          // ランダムUID
  label: string;       // 設定の呼び方（日本語など）
  promptTags: string;  // 対応するプロンプトタグ（カンマ区切り）
  targetBlock: string; // 挿入先ブロックID
  notes: string;       // 備考・補足
  pinned: boolean;     // ピン固定（★）
};
```

### ブロック選択肢

キャラの実際のブロック一覧（`char.blocks`）を優先表示し、  
`char.blocks`に存在しないIDはフォールバック静的リストで補完。

```
face / attribute / body / outfit / feature / effect /
artstyle / composition / background / lighting / quality / negative
（＋キャラ固有のカスタムブロック）
```

存在しないブロックが `targetBlock` に設定されている場合は `⚠️` アイコンを表示。  
挿入ボタン（→）は `disabled`（`opacity-30 cursor-not-allowed`）になる。

### UI動作

- **ソート**: ピン固定行（★）が常に上部に表示。それ以外は追加順
- **インライン編集**: テーブル行のラベル / プロンプトタグ / ブロック / 備考はセルを直接クリックして編集（フォーカスが当たるとインライン入力に変わる）
- **→ 挿入**: 対象ブロックの `text` に `appendTag()` でタグを追記。1.5秒後に ✓ フィードバック表示後リセット
- **📋 コピー**: `row.promptTags` をクリップボードへ
- **★ ピン**: クリックでトグル。ピン行はテーブル先頭へ移動

### 追加フォーム

2×2 グリッドのフォームから追加:

| フィールド | 入力 |
|---|---|
| 設定（日本語） | テキスト input（必須） |
| プロンプトタグ | テキスト input |
| 対象ブロック | select |
| 備考 | テキスト input |

### 制約・現状の限界

- **上限200件**
- 検索・フィルタ機能なし
- 行の並び替え不可（ピン固定のみ）
- 挿入時に重複タグチェックなし（同じタグが重複して追加される可能性あり）
- タグ補完・サジェストなし（自由入力のみ）
- 行にタイムスタンプなし（追加日時が不明）
- 一括挿入（全行まとめて対象ブロックへ）機能なし
- 設定シートのフィールドとの連携なし（別々の入力）

---

## 横断的な懸念点

### データ量・同期コスト

- `char.promptLog` はエントリごとに `blocks` スナップショットを持つ。  
  100件 × ブロック数（~20件）× ブロックテキスト = 数百KB になりうる
- Firestore 1MiB 制限に対して、`toCloudChars()` でスリム化済み（cats/lastRandomPicks除去）
- ただし積極的に使うユーザーはログ上限（100件）に到達しやすい

### UX上の摩擦ポイント

1. **設定シート ↔ タグ対応表の二重管理**: 同じ情報を両方に書く必要がある
2. **プロンプトログの重複**: Auto-logは変化検知がないため、COPYのたびに記録される
3. **タグ対応表の挿入 = 追記のみ**: 既存タグとの重複確認なしで `appendTag` される
4. **復元の一方向性**: ブロック復元はできるが、ログのposText/negTextをそのまま貼り付けることはできない（手動コピー → エディタ貼り付けが必要）
5. **モバイルのTagMap**: テーブルは横スクロール対応だが操作ボタンがホバー依存（`group-hover:opacity-100`）のため、タッチでは見えない

---

## 改善アイデア（Geminiへの問いかけ候補）

- 設定シートのデータを参照して、タグ対応表を半自動生成できないか？
- プロンプトログの重複記録を防ぐには？（内容ハッシュ比較など）
- タグ対応表の「挿入」時に重複を自動スキップするべきか、それとも明示的に通知するべきか？
- ログの100件上限を超えた場合の自動アーカイブ・古いものの自動削除は有効か？
- タグ対応表をキャラ間で共有（コピー）できるようにしたい場合のUI設計は？
- 設定シートの内容をAIに渡してプロンプト提案してもらうフローのUXは？
