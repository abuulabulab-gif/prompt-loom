# ブロックテンプレート仕様書

## 概要

`src/data/templates.js` に定義されたプリセット群。TemplateModal から呼び出し、各ブロックへ一括でタグを適用する。

---

## テンプレートデータ構造

```js
{
  id: string,          // ユニークID（英数字+アンダースコア）
  name: string,        // 日本語カード名
  nameEn: string,      // 英語カード名
  icon: string,        // 絵文字アイコン
  desc: string,        // 日本語説明文（1〜2文、UIに表示）
  descEn?: string,     // 英語説明文（任意）
  sizeHintJa?: string, // 推奨サイズヒント・日本語（任意）
  sizeHintEn?: string, // 推奨サイズヒント・英語（任意）
  negHintJa?: string,  // ネガティブ推奨タグ・日本語（任意）
  negHintEn?: string,  // ネガティブ推奨タグ・英語（任意）
  apply: ApplyMap,     // 適用するブロックとタグの定義
}
```

---

## apply の仕様

### 文字列指定（上書き）

```js
apply: {
  composition: 'upper body, side view, hand in hair',
}
```

該当ブロックの現在のテキストを**完全に置き換える**。`quality` / `artstyle` / `background` など、毎回ゼロから設定したいブロックに使う。

### merge 指定（追記）

```js
apply: {
  body: { mode: 'merge', tags: 'nape, bare shoulders, collarbone' },
}
```

既存のタグに**追記**する。ユーザーがすでに入れたタグを消さない。構図・ボディ・顔など、キャラ固有の設定を残しつつ構図タグを足す場合に使う。

### 使えるブロックキー

| キー | 対応ブロック |
|------|------------|
| `composition` | 構図 |
| `quality` | クオリティ |
| `artstyle` | アートスタイル |
| `body` | ボディ |
| `face` | 表情・顔 |
| `outfit` | 衣装 |
| `outfit_detail` | 衣装ディテール |
| `background` | 背景 |
| `effect` | エフェクト |

---

## テンプレート一覧

### スタイル系

| ID | カード名 | コンセプト |
|----|---------|-----------|
| `anime` | アニメ標準 | セルシェード・品質底上げの定番ベース |
| `photo` | フォトリアル | 写真・実写系質感 |
| `fantasy` | ファンタジー | ゲーム・コンセプトアート風 |
| `chibi` | ちびキャラ | 2〜3頭身デフォルメ |
| `pixelart` | ドット絵 | レトロゲーム風 |

### 設定資料・素材系

| ID | カード名 | コンセプト |
|----|---------|-----------|
| `asset` | 素材・白背景 | 立ち絵・透過素材向け白背景 |
| `sanmenzu` | 三面図 | 正面・横・背面の三面参考図（横長推奨） |
| `chardesign` | キャラ設定シート | キャラバイブル・設定資料 |
| `selfie` | 自撮り | スマホ自撮り構図 |
| `wallpaper` | 壁紙向け構図 | スマホ・PC壁紙用の詳細背景＋光 |

### フェチ構図系

| ID | カード名 | コンセプト |
|----|---------|-----------|
| `nape_lift` | 首筋・髪かき上げ | 横顔から髪をかき上げ、うなじ・首筋・鎖骨の色気 |
| `bare_back` | 振り返り・背中見せ | 背中主役、肩越しの印象的な振り返りワンカット |
| `armpitsleeveless` | 脇見せ・チラ見え | ポーズ固定なし、自然な仕草の「見せちゃった」感 |
| `armpit_closeup` | 脇アップ・全開 | 顔と脇を同時にクローズアップ、脇を広げた状態 |
| `midriff_navel` | へそ見せ・乗りかかり | POVでこちらに乗りかかり、捲れた服からへそ・お腹まわりを見せる |
| `underboob_lowangle` | ローアングル・下胸フォーカス | 真下から見上げ、へそ→下乳→顔の順に捉える |
| `zettairyouiki` | 絶対領域・太もも | お腹から太もも付近のドアップ、絶対領域・食い込み |
| `skintight_detail` | むちむち・肉感フォーカス | 下半身だけムチムチ、全身は太らせない |
| `lowangle_legs` | ローアングル・踏みつけ | 片足持ち上げ足裏をカメラ正面に、真下からの見上げ |
| `footperspective` | 足先フォーカス・座り | 座った状態で足先をカメラ側へ伸ばし、足先と脚線を主役にしつつ全身を映す |
| `birdseye_lie` | 真俯瞰・仰向け | 抱き枕イラスト的な真俯瞰の仰向け全身 |
| `split_balance` | I字バランス | 片脚を真上に伸ばした柔軟ポーズ、バレエ的な脚線美 |

### シチュ構図系

| ID | カード名 | コンセプト |
|----|---------|-----------|
| `prone_elbow` | うつ伏せ・肘つき見上げ | うつ伏せで肘をつき、顔→体→足の奥行きであざとく見上げる |
| `supine_relax` | 仰向け・脚見せリラックス | 仰向け、顔→体→脚の奥行き、目線自由 |
| `floor_sit` | 横座り・片膝抱え | 横視点で片膝を抱え、もう片足を伸ばす。座り姿と脚線の雰囲気が主役 |

### ダイナミック系

| ID | カード名 | コンセプト |
|----|---------|-----------|
| `dynamic_booster` | ダイナミックブースター | 棒立ち・静止ポーズ防止の汎用ブースター |
| `extreme_perspective` | 極端な遠近法 | 手前が極端に大きい強烈パース |
| `rider_kick` | ライダーキック | トクサツ飛び蹴りの真下アングル |
| `wind` | 風・なびき | 髪・衣装のなびきで動きを付与 |
| `cinematic` | シネマティック | 映画的な光と奥行きで格を引き上げる仕上げ用 |

### 極限アングル・クローズアップ系

| ID | カード名 | コンセプト |
|----|---------|-----------|
| `lip_focus` | 口元・リップフォーカス | 唇のみ超クローズアップ |
| `eye_focus` | 瞳・アイフォーカス | 瞳のみ超クローズアップ、虹彩を引き出す |
| `fisheye` | 魚眼レンズ | 球面歪み・超広角のレンズ歪み |
| `from_below` | 煽りアングル | 地面から見上げ、キャラの存在感を最大化 |
| `from_above` | 俯瞰アングル | 斜め上から見下ろし、立ち・動きポーズ対応 |

---

## テンプレート追加時のルール

1. **idはユニークな英数字スネークケース**
2. **applyには必須タグのみ入れる** — 任意タグは余白としてユーザーに委ねる
3. **表情タグは原則入れない** — キャラ依存。例外は`looking at viewer`など構図の一部として機能する場合のみ
4. **衣装の具体物は入れない** — `sportswear`, `short shorts`などは衣装ブロックで個別指定
5. **lighting単体は入れない** — `soft lighting`, `backlighting`などはeffectブロックで別管理
6. **mergeを優先** — 上書きは`quality`/`artstyle`/`background`など完全置換が必要な場合のみ
7. **negHintはカンマ区切りのタグ列** — 文章でなくタグそのもの（`front view, full body`形式）
