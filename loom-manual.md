# LOOM 公式ドキュメント v2.5

> prompt-loom.com — AIイラストプロンプト管理Webアプリ
> 最終更新: 2026-06-09

---

## 目次

1. [アプリ概要](#1-アプリ概要)
2. [UI 正式名称一覧](#2-ui-正式名称一覧)
3. [画面レイアウト](#3-画面レイアウト)
4. [ブロック定義](#4-ブロック定義)
5. [全機能リファレンス](#5-全機能リファレンス)
6. [AIツール対応フォーマット](#6-aiツール対応フォーマット)
7. [キーボードショートカット](#7-キーボードショートカット)
8. [データ管理](#8-データ管理)
9. [AI機能（APIキー連携）](#9-ai機能apiキー連携)
10. [設定一覧](#10-設定一覧)

---

## 1. アプリ概要

LOOMは、AIイラスト生成用のプロンプトをタグベースで組み立てる**無料Webアプリ**です。

| 項目 | 内容 |
|------|------|
| URL | prompt-loom.com |
| 動作環境 | モダンブラウザ（Chrome推奨）|
| インストール | 不要（PWA対応でホーム画面追加も可） |
| アカウント | 不要（クラウド同期にはGoogleログイン必須） |
| データ保存 | ブラウザのIndexedDB（ローカル自動保存） |
| クラウド同期 | Googleログインで Firestore 自動同期 |
| 言語 | 日本語 / 英語（UI全体で切替可） |
| テーマ | ダーク / ライト |

---

## 2. UI 正式名称一覧

### エリア・バー

| 正式名（日） | 正式名（英） | 説明 |
|------------|------------|------|
| **ナビゲーションバー** | Navigation Bar | 最上段。AIツール選択・ランダム生成・バリエーション・各種パネル開閉ボタンを配置 |
| **キャラバー** | Character Bar | ナビバー直下の横帯。キャラタブが並ぶ。ホイールで横スクロール可 |
| **キャラクターパネル** | Character Panel | 左サイドバー（PC）またはスライドイン（スマホ）。アクティブキャラの詳細設定 |
| **ブロックエリア** | Block Area | 画面中央のメイン編集領域。ブロックカードが縦に並ぶ |
| **出力バー** | Output Bar | 画面下部。生成したプロンプトを表示・コピーする固定バー |

### キャラバー内要素

| 正式名（日） | 正式名（英） | 説明 |
|------------|------------|------|
| **キャラタブ（PC）** | Character Tab | キャラ名・絵文字・カラーを表示するピル型タブ。ドラッグで並べ替え |
| **キャラドット（スマホ）** | Character Dot | スマホでの丸型カラードット。タップで選択、長押しドラッグで並べ替え |
| **新キャラボタン** | New Character Button | `+ 新キャラ` — キャラクターを追加 |
| **格納庫ボタン** | Library Button | `📚` — アーカイブ済みキャラを管理 |
| **キャラ一覧ボタン** | Character List Button | `≡` — 全キャラを一覧・検索・並べ替えするモーダルを開く |
| **比較セレクト** | Compare Selector | `🆚 比較` — 出力を2キャラで並べて比較 |

### ブロックカード内要素

| 正式名（日） | 正式名（英） | 説明 |
|------------|------------|------|
| **ブロックカード** | Block Card | 1ブロック分のUI。ヘッダー＋タグエリア＋フッターで構成 |
| **ブロックヘッダー** | Block Header | ブロック名・アイコン・折りたたみボタン・ロックボタンを含む帯 |
| **タグボタン** | Tag Button | タグを1つ表すクリック可能なボタン。選択でプロンプトに追加 |
| **カテゴリアコーディオン** | Category Accordion | タグをカテゴリ単位で折りたたむ見出し行 |
| **ピン留めエリア** | Pinned Tags Area | ★でピン留めしたタグを最上部に集約表示するエリア |
| **強度スライダー** | Strength Slider | ブロック全体のタグ強度（0.8 / 0.9 / 1.0 / 1.1 / 1.2 / 1.3）を設定 |
| **カスタムタグエリア** | Custom Tag Area | ユーザーが追加した独自タグを表示・管理するエリア |
| **ユーザーセクション** | User Section | カスタムブロック内にユーザーが自由に作成できるセクション（例：作品名→キャラ名タグ） |
| **転送ボタン** | Transfer Button | `→` — ブロック内のタグを別キャラへコピー転送 |
| **プリセット保存ボタン** | Preset Save Button | `📌 プリセット保存` — 衣装・構図ブロックの状態をプリセットとして保存 |
| **フォーカスモード** | Focus Mode | 1ブロックを拡大表示。他ブロックを一時非表示にして集中編集 |

### 出力バー内要素

| 正式名（日） | 正式名（英） | 説明 |
|------------|------------|------|
| **ポジティブタブ** | Positive Tab | ポジティブプロンプトの出力・コピー |
| **ネガティブタブ** | Negative Tab | ネガティブプロンプトの出力・コピー |
| **ナチュラルタブ** | Natural Tab | タグを読みやすい自然文に変換して表示 |
| **出力エリア** | Output Area | プロンプトを直接編集できるテキストエリア |
| **コピーボタン** | Copy Button | プロンプトをクリップボードにコピー |
| **出力リサイズハンドル** | Output Resize Handle | 出力バーの高さをドラッグで調整 |

### モーダル・パネル

| 正式名（日） | 正式名（英） | 開き方 |
|------------|------------|--------|
| **コマンドパレット** | Command Palette | `Ctrl+K` |
| **グローバルタグ検索** | Global Tag Search | `Ctrl+F` / `G` キー / ナビバーボタン |
| **キャラ一覧** | Character List Modal | キャラバーの `≡` |
| **キャラクター格納庫** | Character Library | キャラバーの `📚` |
| **カラーメーカー** | Color Maker | ナビバーの 🎨 |
| **特徴メーカー** | Feature Maker | ナビバーの 🎯 |
| **マテリアルメーカー** | Material Maker | ナビバーの 🧵 |
| **シーンコンポーズ** | Scene Compose (Collab) | ナビバーの 🎬（2キャラ以上で有効）|
| **プロンプト逆解析** | Prompt Analyzer | `A` キー / ナビバーボタン |
| **自然文変換** | Natural Language Input | ナビバーボタン |
| **テンプレート** | Template Modal | コマンドパレット / ナビバー |
| **ヒストリー** | History Modal | コマンドパレット / キャラクターパネル |
| **比較パネル** | Compare Panel | 比較セレクトでキャラ選択後に表示 |
| **設定** | Settings Modal | `?` キー / ナビバーの ⚙️ |
| **ブロック健康診断** | Block Health Check | キャラクターパネルのアイコン |
| **サポート** | Support Modal | ナビバーのサポートボタン |

### キャラクターノート内タブ

| 正式名（日） | 正式名（英） | 内容 |
|------------|------------|------|
| **プロフィールシート** | Profile Sheet | キャラ設定の10セクション＋カスタム項目 |
| **プロンプトログ** | Prompt Log | プロンプトの記録・日時管理 |
| **タグ対応表** | Tag Map | タグと設定の対応をまとめた表 |

---

## 3. 画面レイアウト

### PC版

```
┌─────────────────────────────────────────────────────────┐
│  ナビゲーションバー (ツール選択・各種ボタン)                  │
├─────────────────────────────────────────────────────────┤
│  キャラバー [タブ▶タブ▶タブ] [+新キャラ][📚][≡] [🆚比較] │
├────────────┬────────────────────────────────────────────┤
│            │  ブロックエリア                              │
│ キャラク   │  ┌──────────────────┐ ┌──────────────────┐ │
│ ターパネル │  │ ブロックカード    │ │ ブロックカード    │ │
│            │  │ (顔 / 体型 / …)  │ │ (衣装 / 背景 / …)│ │
│ ・名前     │  └──────────────────┘ └──────────────────┘ │
│ ・LoRA     │                                             │
│ ・バージョン│                                            │
│ ・プリセット│                                            │
├────────────┴────────────────────────────────────────────┤
│  出力バー [Positive][Negative][Natural]  [コピー]         │
└─────────────────────────────────────────────────────────┘
```

### スマホ版

```
┌───────────────────────────┐
│  ナビゲーションバー         │
├───────────────────────────┤
│  キャラバー [●][●][●][+] │
├───────────────────────────┤
│  ブロックエリア             │
│  ┌─────────────────────┐  │
│  │ ブロックカード       │  │
│  └─────────────────────┘  │
│         …                 │
├───────────────────────────┤
│  出力バー（キーボード連動）  │
└───────────────────────────┘
```

---

## 4. ブロック定義

プロンプトは以下12種のブロックで構成されます。各ブロックはオン/オフ・非表示・ロック・折りたたみが可能です。

| ID | 表示名（日） | 表示名（英） | 役割 |
|----|------------|------------|------|
| `quality` | 基本品質 | Quality | 品質修飾語・安全性タグ（masterpiece, best quality 等） |
| `artstyle` | スタイル | Art Style | 描画スタイル・色調・レンダリング方式 |
| `attribute` | 特徴 | Attribute | 種族・性別・人数・ケモ耳/幻想パーツ（cat ears, wings 等） |
| `face` | 顔 | Face | 顔・髪型・表情・メイク |
| `body` | 体型 | Body | 体型・肌色・肌質感・ボディフォーカス |
| `outfit` | 衣装 | Outfit | 衣装の種類・スタイル選択（「何を着るか」） |
| `outfit_detail` | 衣装ディテール | Outfit Detail | 衣装の形状/素材/柄/装飾アクセサリー（「どう着るか」） |
| `effect` | エフェクト | Effect | 魔法・パーティクル・天候・フィルムエフェクト |
| `composition` | 構図 | Composition | カメラ距離・角度・ポーズ・視線・手・小物 |
| `background` | 背景 | Background | 場所・時間・天気・季節・雰囲気 |
| `lighting` | ライティング | Lighting | 光源種類・照明スタイル |
| `negative` | ネガティブ | Negative | 生成から除外したいタグ（ネガティブプロンプト） |

> **カスタムブロック**: 上記12ブロックに加え、ユーザーが自由なブロックを追加可能（上限：スマホ3個 / PC5個）。
> カスタムブロック内には **ユーザーセクション** を自由に作成でき、二次創作キャラ管理（作品名→キャラ名タグ）等に活用できます。

---

## 5. 全機能リファレンス

### 5-1. キャラクター管理

| 機能 | 説明 |
|------|------|
| マルチキャラクター | 最大30キャラクターを同時管理 |
| 識別子 | 各キャラに名前・カラー・絵文字を設定 |
| タブ並べ替え | ドラッグ&ドロップでキャラバーの順序を変更（PC: 移動距離8px / スマホ: 長押し250ms後） |
| キャラ一覧 | `≡`ボタンでモーダルを開き、検索・クリックジャンプ・ドラッグ並べ替えが可能 |
| 格納庫（アーカイブ） | 一時的に非表示にしたいキャラを格納。タブバーに表示されなくなる |
| バージョン履歴 | キャラごとにスナップショットを自動保存（最大10件）。いつでも過去状態に復元可能 |
| 複製 | キャラタブの `⊕` ボタンで設定ごと複製 |
| 削除 | キャラタブの `✕` ボタンで削除（確認ダイアログあり） |
| 書き出し / 読み込み | `.loom` ファイルとしてエクスポート・インポート可能 |

### 5-2. ブロック操作

| 機能 | 説明 |
|------|------|
| タグ選択 | タグボタンをクリックしてプロンプトに追加/削除 |
| ★ ピン留め | タグを長押しまたは右クリックでピン留め。ブロック最上部の専用エリアに固定表示 |
| カスタムタグ追加 | 表示名（日本語可）とプロンプトテキストを別々に設定して登録 |
| カスタムタグ編集 | 登録済みカスタムタグの `✎` ボタンでインライン編集 |
| カスタムタグ一括削除 | `全て消す` ボタン |
| タグ強度 | ブロック単位で 0.8 / 0.9 / 1.0 / 1.1 / 1.2 / 1.3 を選択 |
| ブロック有効/無効 | ブロックヘッダーのスイッチでオン/オフ |
| ロック | ロック中はタグ変更・カスタムタグ追加を防止 |
| 非表示 | ブロックをサイドバーの非表示リストに移動（後で復元可能） |
| 折りたたみ | ブロックカードを折りたたんで省スペース化。状態は自動保存 |
| 並べ替え | ブロックカードをドラッグで上下に並べ替え |
| フォーカスモード | ブロック名をダブルクリックで1ブロックを拡大表示 |
| 転送 | `→` ボタンから他のキャラへブロック内容をコピー |
| プリセット保存 | 衣装・構図ブロックの現在の状態をプリセットとして保存・呼び出し |

### 5-3. カスタムブロック

| 機能 | 説明 |
|------|------|
| カスタムブロック追加 | 自由な名前でブロックを作成（上限：スマホ3 / PC5） |
| ユーザーセクション | カスタムブロック内にセクションを追加し、カスタムタグを分類管理 |
| 用途例 | 「二次創作キャラブロック」→「作品名セクション」→「キャラ名のプロンプトタグ」 |
| 保存 | カスタムブロックの内容はIndexedDBとクラウドに自動同期 |

### 5-4. プロンプト出力

| 機能 | 説明 |
|------|------|
| ポジティブ出力 | 有効ブロックのタグを選択中AIツールの記法で整形して出力 |
| ネガティブ出力 | ネガティブブロックのタグを出力 |
| ナチュラル出力 | タグを読みやすい自然文（日/英）に変換して出力 |
| 直接編集 | 出力エリアをクリックして自由にテキスト編集後コピー可能 |
| ワンクリックコピー | コピーボタンでクリップボードに転送 |
| リサイズ | 出力バーの高さをドラッグで調整 |

### 5-5. タグ破綻チェック（コンフリクト検出）

リアルタイムで矛盾するタグの組み合わせを検出し、タグボタン上に警告を表示します。

| 検出カテゴリ例 |
|--------------|
| 髪の長さの矛盾（short hair + very long hair） |
| 種族の矛盾（mermaid + boots / elf + cat ears） |
| ポーズの矛盾（sitting + jumping / lying down + standing） |
| 性別の矛盾（male + 女性向け胸サイズタグ） |
| 天候の矛盾（sunny + rainy + snowing の複数同時指定） |
| 背景の矛盾（indoor + outdoor の同時指定） |
| 翼の種類の重複（dragon wings + angel wings 等） |

- ルール数：100件以上
- おまかせランダム生成にも同ルールが適用され、矛盾ゼロのプロンプトを自動生成

### 5-6. ランダム生成

| 設定 | 説明 |
|------|------|
| **キャラ特化モード** | 顔・髪・体型・衣装・特徴を中心にランダム生成。構図・背景は最小限 |
| **イラストモード** | 構図・ポーズ・背景・ライティングも含めた全体的なランダム生成 |

- 競合ルールに従い矛盾タグを自動除外
- 種族（ケモノ耳・幻想パーツ等）が選ばれた場合、対応パーツを自動付与
- カラーメーカーのロジックと連動し色の組み合わせも生成

### 5-7. バリエーション

現在のプロンプトから、差分のみを入れ替えた3つのバリエーションを生成。

| 差し替え対象 |
|------------|
| ヘア（色・スタイル） |
| 衣装 |
| オプションタグ（効果・アクセサリー等） |

- 任意のバリエーションをワンクリックでブロックに適用してそのまま編集継続可能

### 5-8. メーカー（Maker）モーダル

**カラーメーカー（Color Maker）**
- 髪・瞳・衣装・肌・アクセサリー等のカラーをGUIで選択して適用
- インナーカラー・ヘテロクロミア（左右異なる瞳色）にも対応
- 適用先はブロックに自動振り分け

**特徴メーカー（Feature Maker）**
- 種族・体のパーツ・幻想系特徴をカテゴリ選択で一括適用
- ランダム生成と同じロジックで整合性を保って適用

**マテリアルメーカー（Material Maker）**
- 衣装の素材・テクスチャ・仕上げ効果を選択して衣装ブロックに適用

### 5-9. シーンコンポーズ（Collab）

2体以上のキャラクターを選んで合成プロンプトを生成。複数キャラが登場するシーンに使用。

### 5-10. プロンプト逆解析

出力プロンプト（またはペーストしたプロンプト）を解析し、どのブロック・カテゴリに対応するかを可視化。

### 5-11. グローバルタグ検索

全ブロックをまたいでタグを検索。検索結果からそのままタグをON/OFF可能。カスタムタグも検索対象に含まれる。

### 5-12. キャラクターノート

エディタタブと並んで存在するキャラクター専用のノートエリア。

| タブ | 内容 |
|------|------|
| プロフィールシート | 身長・体重・年齢・性格・設定・設定メモなど10セクション＋カスタム項目 |
| プロンプトログ | 日時付きでプロンプトを記録・管理 |
| タグ対応表 | タグと設定の対応を一覧表示 |

### 5-13. ブロック健康診断

現在のプロンプト設定を分析し、潜在的な問題点を指摘するパネル。

- 競合タグの検出
- 胸サイズ未設定・性別設定の一貫性チェック
- 過剰タグ・重複タグの警告

---

## 6. AIツール対応フォーマット

| ツール | 強度の書き方 | 備考 |
|--------|------------|------|
| **MJ** (Midjourney) | `tag::1.2` | `::` で強調 |
| **NAI** (NovelAI) | `{tag}` / `[[tag]]` | `{}` で強調、`[[]]` で弱化 |
| **SD** (Stable Diffusion) | `(tag:1.2)` | 括弧内に倍率 |
| **Flux** | `tag` | シンプル出力（強度非対応） |
| **DALL-E** | `tag` | シンプル出力 |

---

## 7. キーボードショートカット

| キー | 動作 |
|------|------|
| `Ctrl+K` | コマンドパレットを開く |
| `Ctrl+F` / `G` | グローバルタグ検索を開く |
| `?` | 設定モーダルを開く |
| `A` | プロンプト逆解析を開く |
| `P` | ポジティブタブへ切替 |
| `N` | ネガティブタブへ切替 |
| `1`〜`5` | AIツールを切替（MJ/NAI/SD/Flux/DALL-E） |
| `Escape` | 最前面のモーダル/パネルを閉じる |

---

## 8. データ管理

### ローカル保存

- **エンジン**: Dexie（IndexedDB）
- **保存タイミング**: 変更のたびに自動保存（デバウンス処理あり）
- **保存データ**: キャラクター全件・設定・テーマ・言語・ツール選択・出力高さ・ヒストリー

### クラウド同期

- **サービス**: Firebase Firestore
- **条件**: Googleアカウントでサインイン時のみ動作
- **構造**: `/users/{uid}/data/state`（1ドキュメント）
- **競合解決**: last-write-wins（`orderUpdatedAt` / `settingsUpdatedAt` タイムスタンプ比較）
- **同期内容**: ローカル保存と同一

### ファイル書き出し（バックアップ）

- `.loom` ファイル（JSON形式）として任意のキャラをエクスポート
- 他ユーザーへの共有も可能
- 設定とは独立して動作

### キャラクターデータ構造（概要）

```js
{
  id: string,          // 一意のID
  name: string,        // キャラ名
  color: string,       // 識別カラー（HEX）
  emoji: string,       // 識別絵文字
  blocks: Block[],     // 12ブロック + カスタムブロック
  hiddenBlocks: string[],  // 非表示ブロックIDリスト
  versions: Version[], // バージョン履歴（最大10件）
  loras: Lora[],       // LoRA情報
  profile: ProfileData,// キャラクターノートのプロフィール
  costumePresets: Preset[],  // 衣装プリセット
  shotPresets: Preset[],     // 構図プリセット
  memo: string,        // メモ
}
```

---

## 9. AI機能（APIキー連携）

APIキー（Claude または OpenAI）を設定モーダルで登録することで解放されます。

| 機能名 | 説明 | 必要API |
|--------|------|---------|
| **タグ提案** | 現在の選択タグからAIが次のタグを推薦 | Claude / OpenAI |
| **自然文→タグ変換** | 「赤い服の明るい魔法少女」のような自然文をタグに自動分解 | Claude / OpenAI |
| **プロンプト整形** | 選択タグをAIが自然なプロンプト表現にブラッシュアップ | Claude / OpenAI |
| **画像解析→タグ抽出** | 参考画像をアップロードしてタグを自動抽出 | Claude |

---

## 10. 設定一覧

| 設定項目 | 選択肢 |
|---------|--------|
| テーマ | ダーク / ライト |
| 言語 | 日本語 / English |
| 表示モード | シンプル / ノーマル / エキスパート |
| ランダムモード | キャラ特化 / イラスト |
| APIキー（Claude） | 任意入力 |
| APIキー（OpenAI） | 任意入力 |
| クラウド同期 | Googleサインイン / サインアウト |
| 非表示ブロック | 個別復元 / 全て復元 |
| ショートカット一覧 | 設定モーダル内に常時表示 |

### 表示モードの違い

| モード | 表示ブロック |
|--------|------------|
| **シンプル** | 品質・スタイル・特徴・顔・体型・衣装・背景・ネガティブ（8ブロック）＋カスタムブロック |
| **ノーマル** | 全12ブロック＋カスタムブロック |
| **エキスパート** | ノーマルと同じブロック＋詳細設定パネルの追加表示 |

---

*このドキュメントはLOOM v2.5（2026-06-09時点）に基づいています。*
